Running Executable jar file

java -jar AkshayRane_SSAProject.jar

IF RUNNING THE JAVA FILES

• Launch the most recent version of Eclipse.
• Create or open an existing project, then add all the Java files to the default package within 	that project.
• Build and compile the project to ensure there are no errors.
• Next, execute the following Java file:
	1.	Main.java
• When prompted, provide any necessary input parameters as required by the program.
• The results will be shown in the output console located at the bottom of the Eclipse workspace. 